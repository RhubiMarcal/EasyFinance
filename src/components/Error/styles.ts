import styled from 'styled-components'
import { Colors } from '../../styles'

export const ErrorContainer = styled.p`
  color: ${Colors.red};
  font-size: 22px;
  font-weight: bold;
  position: absolute;
  bottom: 6px;
`
